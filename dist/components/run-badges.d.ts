import type { ReactNode } from 'react';
import type { RunState } from '../types';
export interface RunBadgesProps {
    run: RunState;
}
export declare function RunBadges({ run }: RunBadgesProps): ReactNode;
